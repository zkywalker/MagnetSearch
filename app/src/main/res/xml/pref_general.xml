<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
      dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:icon="@drawable/ic_color_lens_black_24dp"
        android:defaultValue="-1"
        android:entries="@array/pref_theme_titles"
        android:entryValues="@array/pref_theme_values"
        android:key="@string/key_theme"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_theme" />

    <ListPreference
        android:icon="@drawable/ic_search_black_24dp"
        android:defaultValue="-1"
        android:entries="@array/pref_search_source_titles"
        android:entryValues="@array/pref_search_source_values"
        android:key="@string/key_search_source"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_search_source"
        android:summary="@string/pref_summary_search_source"
        />

    <!-- A 'parent' preference, which enables/disables child preferences (below)
     when checked/unchecked. -->
    <SwitchPreference
        android:icon="@drawable/ic_open_in_browser_black_24dp"
        android:defaultValue="true"
        android:key="@string/key_fast_open"
        android:title="@string/pref_title_fast_open" />
    <SwitchPreference
        android:icon="@drawable/ic_border_clear_black_24dp"
        android:defaultValue="true"
        android:key="@string/key_share_qr"
        android:title="@string/pref_title_share_qr" />

    <Preference
        android:icon="@drawable/ic_delete_black_24dp"
        android:key="@string/key_clean"
        android:title="@string/pref_title_clean_history"
        />

    <Preference
        android:icon="@drawable/ic_favorite_black_24dp"
        android:key="@string/key_score"
        android:title="@string/pref_title_score"
        />





</PreferenceScreen>
